@model BLL.Models.SubcategoryViewModel

@{
    ViewBag.Title = "Subcategories";

    string links = Html.PageLinks(Model.CurrentPage, Model.TotalPages, x => Url.Action("Index", "Subcategory", new
    {
        page = x,
        items = Model.Items,
        searchName = Model.SearchName
    }));
}

<h2>Subcategories</h2>

@using (Html.BeginForm())
{
    @Html.Partial("_ItemsPerPage", Model)
    @Html.Partial("_Search", Model)
}

<p>
    @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-info" })
</p>

@Html.Raw(links)
@Html.Partial("_Subcategories", Model.Subcategories)
@Html.Raw(links)